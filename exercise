Break statement

#include<stdio.h>

int main() {
    int i;
    int a[10];
    int sum=0;
    printf("\nEnter 10 numbers");
    for(i=0;i<10;i++)
    {
        scanf("%d",&a[i]);
        sum=sum+a[i];
        if(a[i]<0)
        {
            break;
        }
    }
     printf("sum: %d",sum);
}
